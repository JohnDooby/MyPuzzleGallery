/**
 * Configuration d'environnement (production / défaut).
 * Les valeurs réelles de dev sont dans environment.development.ts (non versionné).
 */
export const environment = {
  production: true,
  supabaseUrl: '',
  supabaseAnonKey: '',
};
