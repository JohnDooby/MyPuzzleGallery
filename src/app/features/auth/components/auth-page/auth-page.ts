import { Component } from '@angular/core';

/**
 * Page compte (stub) — login / inscription à venir (E04).
 */
@Component({
  selector: 'app-auth-page',
  imports: [],
  templateUrl: './auth-page.html',
  styleUrl: './auth-page.scss',
})
export class AuthPage {}
